{
  "name": "treezone-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "cd ../client && npm install && npm run build",
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.19.0",
    "@anthropic-ai/sdk": "^0.30.0",
    "@clerk/express": "^1.0.0",
    "express-rate-limit": "^7.3.0",
    "helmet": "^7.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express-validator": "^7.1.0"
  }
}
